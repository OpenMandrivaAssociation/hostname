diff -up hostname-debian-3.25/Makefile.1~ hostname-debian-3.25/Makefile
--- hostname-debian-3.25/Makefile.1~	2024-10-11 08:18:27.000000000 +0200
+++ hostname-debian-3.25/Makefile	2026-01-13 21:02:30.774686096 +0100
@@ -14,10 +14,6 @@ OBJS=hostname.o
 
 hostname: $(OBJS)
 	$(CC) $(CFLAGS) -o $@ $(OBJS) $(LDFLAGS)
-	ln -fs hostname dnsdomainname
-	ln -fs hostname domainname
-	ln -fs hostname ypdomainname
-	ln -fs hostname nisdomainname
 
 install: hostname
 	$(INSTALL) -d $(DESTDIR)$(mandir)/man1
diff -up hostname-debian-3.25/hostname.1.1~ hostname-debian-3.25/hostname.1
--- hostname-debian-3.25/hostname.1.1~	2024-10-11 08:18:27.000000000 +0200
+++ hostname-debian-3.25/hostname.1	2026-01-13 21:02:08.241609702 +0100
@@ -80,6 +80,28 @@ complete FQDN of the system is returned
 .B THE FQDN
 below).
 
+.LP
+The function 
+.BR gethostname(2)
+is used to get the hostname.  When the 
+.BR "hostname \-a, \-d, \-f or \-i" 
+is called will 
+.BR gethostbyname(3)
+be called.  The difference in 
+.BR gethostname(2)
+and
+.BR gethostbyname(3)
+is that
+.BR gethostbyname(3)
+is network aware, so it consults 
+.IR /etc/nsswitch.conf
+and
+.IR /etc/host.conf
+to decide whether to read information in
+.IR /etc/hostname
+or
+.IR /etc/hosts
+
 .SS "SET NAME"
 When called with one argument or with the
 .B \-\-file
@@ -105,8 +127,7 @@ command (see
 .B THE FQDN
 below).
 .LP
-The host name is usually set once at system startup in
-.I /etc/init.d/hostname.sh
+The host name is usually set once at system startup
 (normally by reading the contents of a file which contains
 the host name, e.g.
 .IR /etc/hostname ).
